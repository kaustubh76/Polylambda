# Slim RUNTIME deps for the containerized dashboard (paper-mode, read-only over the engine).
# Intentionally a subset of the repo's requirements.txt:
#   - omits scikit-learn/scipy  → LoadedHazard is pure-python; sklearn is lazy-imported for TRAINING
#     only, never at request time (the model is loaded from JSON).
#   - includes web3 (below) → the Amoy TESTNET path (engine signs quotes/dispute/resolve on-chain,
#     bypassing the mainnet gate); pulls eth-account transitively.
#   - still omits polymarket-client → the gated mainnet CLOB live-trade write path, never touched here.
# Result: an all-wheels install (no compiler), a small image, fast cold start.
fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.26
pandas>=2.1          # disputes explorer reads dataset_release/*.parquet
pyarrow>=16          # parquet engine for pandas
duckdb>=1.1          # dispute-count numerators over the released parquet (falls back to constants if absent)
python-dotenv>=1.0
web3>=6.15,<8        # backend Amoy testnet signing + reads (webapp/backend/chain.py); pulls eth-account
